import 'package:flutter/material.dart';

class KColors {
  static const Color primaryColor = Color(0xFF292F3B);
  static const Color hoverColor = Color.fromARGB(255, 69, 79, 99);
  static var positiveColor = Colors.green[700] ?? Colors.green;
  static var negativeColor = Colors.redAccent[700] ?? Colors.red;
}
